using System;
namespace ALauncher.Core;

public static class ManagerHelper
{
    public static readonly string WorkFolder = $"{Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData)}/ALauncher";
}